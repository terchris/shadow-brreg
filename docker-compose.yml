
# start this with docker compose up -d
version: '3.8'
services:
  app:
    container_name: shadow-app
    image: node:lts-alpine
    command: ["sh","-c","wget -O https://raw.githubusercontent.com/terchris/shadow-brreg/main/shadow-app-setup.sh shadow-app-setup.sh && chmod +x shadow-app-setup.sh && ./shadow-app-setup.sh"]    
    
    

#docker exec -it shadow-app sh
# docker container run -it shadow-app /bin/bash


#services:
#  app:
#    container_name: shadow-app
#    image: node:lts-alpine
#    command: ["sh","-c","apk add git && npm install -g typescript && git clone --no-hardlinks https://github.com/terchris/shadow-brreg  && cd /shadow-brreg/app/shadow && chmod +x ./shadow-setup.sh && ./shadow-setup.sh"]    

#services:
#  app:
#    container_name: shadow-app
#    build: app/shadow
#    command: ["sh","-c","git clone --no-hardlinks https://github.com/terchris/shadow-brreg /shadow-brreg && cd /shadow-brreg/app/shadow && chmod +x ./shadow-appinstall.sh && ./shadow-appinstall.sh"]    



#docker-compose exec shadow-app sh
#docker exec -ti shadow-app /bin/bash